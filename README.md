# Bounce Club

Fresh rebuild — design follows Figma Make + `design/REQUIREMENTS.md`.

```bash
npm install
cp .env.example .env.local
npm run dev   # http://localhost:3004
npm run gallery:process
```

Default admin (local):
- username: `DikkiX`
- password: `admin`

Docs:
- `design/REQUIREMENTS.md` — all functionaliteit
- `design/FIGMA-MAKE-BRIEF.md` — design prompts
